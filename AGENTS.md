# Agent 开发入口

修改代码前，自行检查目标代码和相关业务模块，明确必要假设、修改范围与验证方式。只有信息缺失且会实质影响方案、安全性或兼容性时，才向用户确认。

涉及目录结构、依赖、公共组件或类型、路由与权限、全局状态、全局样式时，按需阅读 [FRONTEND_ENGINEERING_STANDARD.md](./FRONTEND_ENGINEERING_STANDARD.md) 的相关章节。局部文案、测试数据和组件内样式修改无需读取无关章节。

遵循最小变更原则，不修改、回滚或格式化与任务无关的代码。完成后运行项目中已存在且与改动相关的最小验证；当前没有更具体脚本时运行 `npm run build`。按风险比例报告改动、验证结果和遗留风险，纯只读任务无需执行代码验证。
