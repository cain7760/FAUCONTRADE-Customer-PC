# 前端工程代码结构标准

> 适用范围：当前 Vue 3 + Vite + Element Plus 项目中涉及架构、公共能力或跨模块影响的前端改动。
> 当前仓库以 JavaScript 为主。TypeScript 规则仅适用于已经启用 TypeScript 的模块；迁移必须作为明确任务进行，不在普通改动中顺手实施。

## 1. 目标

本规范的目标不是增加目录数量，而是让代码具备以下特征：

- 业务代码按领域聚合，开发者可以快速定位功能。
- 页面、业务组件、通用组件、数据请求和状态管理职责清晰。
- 公共第三方组件库统一接入、统一封装、统一升级。
- 模块之间依赖方向稳定，避免循环依赖和跨业务污染。
- Agent 编写代码前能够明确假设、修改边界和验证方式。
- 代码易测试、易回滚、易删除，适合多人并行开发。

## 2. 开发与验证流程

修改代码前，Agent 应自行检查与当前任务相关的项目事实：

1. 首次接触项目或相关配置发生变化时，检查框架、构建工具、包管理器和语言配置。
2. 目标功能所属的业务域，以及是否已有相同或相近模块。
3. 是否可以复用现有组件、Hook、API 方法或类型。
4. 修改范围、关键假设、潜在影响和验证命令。
5. 是否涉及公共组件、全局样式、路由、权限或全局状态。

低风险、局部且可逆的选择采用现有约定或合理默认值直接处理。只有缺失信息会实质改变功能、安全性、数据、外部副作用或兼容性时，才向用户确认。

不允许在未检查现有实现的情况下新建重复组件、重复请求方法或重复类型。

## 3. 推荐目录结构

```text
dist/                            # Vite 构建产物；不提交、不手动修改
public/                          # 原样静态资源
src/
├── api/                         # 按领域拆分的接口请求与接口数据转换
├── assets/                      # 需要经过构建处理的图片、字体等资源
├── components/                  # 跨页面复用组件；包含 UI 组件库适配层
├── config/                      # 环境、业务开关、第三方服务等配置
├── directives/                  # 全局或可复用 Vue 指令
├── hooks/                       # 跨页面复用的组合逻辑，统一使用 useXxx 命名
├── interface/                   # 接口请求/响应及领域模型的 TypeScript 类型
├── layout/                      # 全局页面框架与布局组件
├── locales/                     # 国际化语言包与配置
├── plugins/                     # 第三方插件注册与初始化
├── router/                      # 路由定义、守卫与路由元信息
├── store/                       # 跨页面共享的客户端状态
├── style/ 或根级样式文件         # 设计令牌、全局样式及组件库主题覆盖；沿用现有位置
├── utils/                       # 无副作用的通用纯函数
├── views/                       # 路由页面，按业务域建立子目录
│   ├── data/
│   ├── error/
│   ├── inquiry/
│   └── login/
├── App.vue
├── auto-imports.d.ts            # 启用 TypeScript 自动导入时由工具维护
├── components.d.ts              # 启用 TypeScript 组件自动注册时由工具维护
└── main.js / main.ts
types/                           # 启用 TypeScript 时的全局声明文件，不放具体业务类型
├── directives.d.ts
├── global-components.d.ts
├── global.d.ts
├── index.d.ts
├── router.d.ts
└── shims-tsx.d.ts
```

### 目录选择规则

- 路由页面或独立 HTML 入口对应的页面组件放在 `views/<domain>/`；页面私有组件可放在其 `components/` 子目录。
- 被两个及以上页面稳定复用的组件、逻辑或函数，才分别提取到 `components/`、`hooks/` 或 `utils/`。
- 页面只负责组合布局和编排，不承载大段请求、数据转换或可复用业务规则。
- `api/` 按业务域拆分，例如 `api/order.js` 或 `api/order.ts`，不在页面中直接调用 HTTP 客户端。
- 启用 TypeScript 时，`interface/` 按业务域定义类型；全局 `.d.ts` 声明只放根目录 `types/`。
- `utils/` 不得依赖 Vue、路由、Store 或具体业务。
- `hooks/` 可依赖 Vue 响应式能力，但不得直接渲染 UI 或依赖具体页面。
- 不创建含义模糊的 `common/`、`misc/`、`helpers/` 大杂烩目录。

## 4. 业务模块标准

页面功能应按以下结构组织：

```text
views/<domain> → components / hooks → api / store
                ↘ interface / utils / config
```

- `views/`：路由页面，负责页面级布局、权限和模块组合。
- `components/`：通用展示和交互组件；业务私有组件应靠近对应页面。
- `hooks/`：可复用的状态、行为和副作用，使用 `useXxx` 命名。
- `api/`：只负责接口调用和接口数据转换，不直接操作 DOM。
- `interface/`：接口模型、领域类型和枚举；不放全局声明。
- `store/`：仅管理跨页面共享且生命周期明确的客户端状态。

## 5. 公共第三方组件库规范

项目已经使用 Element Plus。当组件库能力满足交互、性能和视觉要求时优先使用；交易专用或组件库能力不足的控件可以按现有模式实现。

### 5.1 统一入口

第三方组件库的注册、主题变量、全局配置和版本升级应在统一位置维护，并沿用当前项目已有入口：

```text
src/components/ui/           # 统一适配和二次封装
src/style/ 或现有根级样式文件 # 主题变量和组件样式覆盖
src/plugins/                 # 全局注册和初始化
```

### 5.2 何时封装

仅在以下情况封装公共组件：

- 需要统一项目视觉或交互规范。
- 需要统一默认属性、错误处理或埋点。
- 第三方 API 直接暴露会导致业务代码大量重复。
- 需要隔离未来替换第三方组件库的影响。

封装组件必须：

- 使用项目统一命名，例如 `AppTable`、`AppDialog`、`AppSelect`。
- 保留必要的原组件能力，不得无故删减属性和事件。
- 写明与原组件的差异和适用场景。
- 避免把具体业务字段写死在公共组件中。

### 5.3 禁止事项

- 同一项目同时引入多个功能相同的 UI 组件库。
- 在页面中散落第三方组件的特殊样式覆盖。
- 为一次性页面需求创建全局公共组件。
- 直接修改 `node_modules` 或复制第三方源码进入项目。
- 未经评估自行升级公共组件库版本。

## 6. 依赖与导入规则

依赖方向必须从高层指向低层，禁止低层反向依赖业务页面：

```text
views / layout / router
    ↓
components / hooks
    ↓
api / store / plugins
    ↓
utils / interface / config / third-party libraries
```

- 禁止跨 `views/<domain>` 直接引用对方的私有组件。
- 跨页面复用必须通过 `components/`、`hooks/`、`api/` 或其他公共层完成。
- 禁止出现循环依赖。
- 优先使用路径别名，避免多层 `../../..`。
- 一个文件只从一个稳定入口导入同一模块。

## 7. 状态、请求与数据转换

- 页面局部交互状态使用组件自身状态。
- 仅在多个业务域共享且生命周期明确时使用全局 Store。
- 服务端数据、加载状态、错误状态和缓存策略不得随意复制到多个 Store。
- API 返回结构应在 API 层或适配层转换为前端领域模型。
- 页面不直接依赖后端字段细节，避免把接口变更扩散到整个 UI。
- 所有异步操作按场景考虑加载中、空数据和失败状态。读操作按需提供重试；写操作仅在具备幂等键或明确安全策略时重试，交易提交不得默认自动重试。

## 8. 组件与命名规范

- Vue 组件使用 `PascalCase.vue`，例如 `OrderSummary.vue`。
- Hook 使用 `useXxx.js` 或 `useXxx.ts`，与所在模块语言保持一致。
- API 方法使用动词开头，例如 `fetchOrders`、`createOrder`。
- 布尔值使用 `is`、`has`、`can`、`should` 开头。
- 事件处理函数使用 `handleXxx`，但业务动作应使用更具体的名称。
- 避免为顶层文件、核心领域对象或跨模块 API 使用 `data`、`info`、`temp`、`common` 等脱离上下文后无法表达意图的命名；局部且语义明确的参数不受此限。
- 单个组件过长或同时包含多种独立职责时，优先拆分业务组件、Hook 或纯工具函数。

## 9. 样式规范

- 优先使用组件库能力和项目设计令牌。
- 全局样式放在 `src/style/` 或当前已有的根级全局样式文件中，业务样式跟随业务组件或页面；目录迁移应作为独立任务。
- 禁止使用无法解释的大量魔法数字；间距、颜色、字号优先使用令牌。
- 样式覆盖必须限制作用域，避免影响其他页面。
- 不使用 `!important` 解决常规优先级问题；确需使用时必须写明原因。
- 组件样式不得依赖页面层级选择器才能正常工作。

## 10. 多 Agent 协作规则

开发与验证统一遵循第 2 节，不重复执行另一套检查。多人或多 Agent 并行时额外遵守：

1. 不覆盖、不回滚其他 Agent 或用户已有改动。
2. 修改公共组件、公共类型、全局样式或基础服务时，说明影响面。
3. 格式化、重命名和功能修改保持独立边界，避免扩大冲突面。

推荐提交边界：一个任务对应一个清晰的业务变更；格式化、重命名和功能修改不要混在同一个变更中。

## 11. 完成前检查清单

以下清单按改动范围选用，不要求检查与任务无关的项目部分。

### 结构

- [ ] 代码放在正确的业务域或公共层。
- [ ] 没有新增重复组件、重复 API 或重复类型。
- [ ] 没有跨业务域引用内部实现。
- [ ] 没有引入循环依赖或模糊目录。

### 功能

- [ ] 已处理加载、空数据、错误和异常输入。
- [ ] 请求、状态和数据转换职责清晰。
- [ ] 权限和路由行为符合现有约定。

### 质量

- [ ] 类型、命名和导入符合规范。
- [ ] 公共组件改动已评估兼容性。
- [ ] 已运行 `package.json` 中存在且与改动相关的最小验证；当前没有更具体脚本时运行 `npm run build`。
- [ ] 变更范围没有包含无关文件。

## 12. Agent 输出模板

复杂、跨模块或高风险改动使用以下结构。简单改动可合并为简短的改动说明、验证结果和遗留风险，无需机械填写空章节。

```text
假设：
- ...

修改范围：
- ...

实现说明：
- ...

验证：
- 命令：...
- 结果：通过 / 未通过（原因）

风险与后续：
- ...
```

## 13. 例外处理

当现有代码与本规范冲突时，Agent 不应大范围迁移目录。应当：

1. 保持本次任务的修改边界。
2. 在现有结构中采用最接近本规范的写法。
3. 在输出中记录冲突点和建议的后续迁移方案。

规范本身需要修改时，应说明兼容性影响。若项目已有团队评审流程则遵循该流程；用户明确要求修改时无需额外重复确认。
